{
  "mongo": {
    "Connectionstring": "{{CONNECTION_STRING}}",
    "Database": "{{DATABASE}}"
  },
  "sentry": {
    "Dsn": "{{SENTRY_DSN}}"
  },
  "token": {
    "PublicKey": "{{JWT_SIGNING_PUBLIC_KEY}}",
    "Issuer": "{{JWT_ISSUER}}",
    "AvailableAudience": "{{JWT_AUDIENCE}}"
  }
}
